@{ 
    string action = ViewContext.RouteData.Values["Action"].ToString();
    string controller = ViewContext.RouteData.Values["Controller"].ToString();
}

<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <div class="container">
        <a href="~/#" class="navbar-brand">
            <img src="~/img/brand.svg" height="30" />
            Record Store
        </a>
        <button class="navbar-toggler" data-target="#navbarSupportedContent" data-toggle="collapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link @(controller == "Records" && action == "List" ? "active" : "")" asp-controller="Records" asp-action="List">Records</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(controller == "Artists" && action == "List" ? "active" : "")" asp-controller="Artists" asp-action="List">Artists</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(controller == "About" && action == "Index" ? "active" : "")" asp-controller="About" asp-action="Index">About</a>
                </li>
            </ul>
            <button class="btn btn-dark">Cart</button>
        </div>
    </div>
    
</nav>